spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5433}/${DB_NAME:datacollector}
spring.datasource.username=${DB_USER:datacollector}
spring.datasource.password=${DB_PASSWORD:datacollector}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false

server.port=${SERVER_PORT:8081}