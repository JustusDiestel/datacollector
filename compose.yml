services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: datacollector
      POSTGRES_PASSWORD: datacollector
      POSTGRES_DB: datacollector
    ports:
      - 5433:5432
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: